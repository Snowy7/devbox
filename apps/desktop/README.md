# Devbox Desktop

This is the placeholder Electron + React + TypeScript shell for Devbox.

The desktop app will present project status, timelines, policy decisions, and restore flows. It must call the Rust daemon for all workspace mutations; it should never write, delete, or restore project files directly.
