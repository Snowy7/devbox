[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/hosted/devbox-api.Dockerfile"

[deploy]
healthcheckPath = "/ready"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
